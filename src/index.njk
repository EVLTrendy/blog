---
title: Your Hub for Social Media, Growth, & Digital Strategy
description: Discover actionable insights, proven strategies, and expert resources for social media marketing, content creation, and digital growth. Your go-to resource hub for online success.
layout: 'base.njk'
---

{# ============================================
   HERO SECTION - High Impact Value Proposition
   ============================================ #}
<section class="hero-section">
  <div class="hero-content">
    <h1 class="hero-title">{{ homepage.hero.title }}</h1>
    <p class="hero-subtitle">{{ homepage.hero.subtitle }}</p>
    <p class="hero-description">
      {{ homepage.hero.description }}
    </p>
    <a href="#categories" class="hero-cta">{{ homepage.hero.ctaText }}</a>
    
    <div class="hero-stats">
      {% for stat in homepage.hero.stats %}
      <div class="stat-item">
        <span class="stat-number">{{ stat.number }}</span>
        <span class="stat-label">{{ stat.label }}</span>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

{# ============================================
   CATEGORY HUB - Resource Panels
   ============================================ #}
<section class="categories-hub" id="categories">
  <div class="categories-container">
    <div class="hub-intro">
      <h2>{{ homepage.categories.title }}</h2>
      <p>
        {{ homepage.categories.description }}
      </p>
    </div>
    
    <div class="category-grid">
      {# Social Media Marketing Category #}
      <div class="category-panel">
        <div class="category-header">
          <div class="category-icon">SM</div>
          <div class="category-title-group">
            <h3>Social Media Marketing</h3>
            {% set smCount = collections.blog | filterByCategory('social-media') | length %}
            <span class="category-count">{{ smCount }} Article{% if smCount != 1 %}s{% endif %}</span>
          </div>
        </div>
        <p class="category-description">
          {{ homepage.categories.panels.socialMedia.description }}
        </p>
        <div class="category-posts">
          {% set socialMediaPosts = collections.blog | filterByCategory('social-media') | reverse | limit(3) %}
          {% if socialMediaPosts.length > 0 %}
            {% for post in socialMediaPosts %}
            <div class="category-post-item">
              <a href="{{ post.url }}" class="category-post-link">{{ post.data.title }}</a>
            </div>
            {% endfor %}
          {% else %}
            <p class="category-empty">No posts in this category yet.</p>
          {% endif %}
        </div>
        <a href="/blog/?category=social-media" class="view-all-link">View All Posts</a>
      </div>
      
      {# Content Creation Category #}
      <div class="category-panel">
        <div class="category-header">
          <div class="category-icon">CC</div>
          <div class="category-title-group">
            <h3>Content Creation</h3>
            {% set ccCount = collections.blog | filterByCategory('content-creation') | length %}
            <span class="category-count">{{ ccCount }} Article{% if ccCount != 1 %}s{% endif %}</span>
          </div>
        </div>
        <p class="category-description">
          {{ homepage.categories.panels.contentCreation.description }}
        </p>
        <div class="category-posts">
          {% set contentPosts = collections.blog | filterByCategory('content-creation') | reverse | limit(3) %}
          {% if contentPosts.length > 0 %}
            {% for post in contentPosts %}
            <div class="category-post-item">
              <a href="{{ post.url }}" class="category-post-link">{{ post.data.title }}</a>
            </div>
            {% endfor %}
          {% else %}
            <p class="category-empty">No posts in this category yet.</p>
          {% endif %}
        </div>
        <a href="/blog/?category=content-creation" class="view-all-link">View All Posts</a>
      </div>
      
      {# SEO & Analytics Category #}
      <div class="category-panel">
        <div class="category-header">
          <div class="category-icon">SEO</div>
          <div class="category-title-group">
            <h3>SEO & Analytics</h3>
            {% set seoCount = collections.blog | filterByCategory('seo') | length %}
            <span class="category-count">{{ seoCount }} Article{% if seoCount != 1 %}s{% endif %}</span>
          </div>
        </div>
        <p class="category-description">
          {{ homepage.categories.panels.seo.description }}
        </p>
        <div class="category-posts">
          {% set seoPosts = collections.blog | filterByCategory('seo') | reverse | limit(3) %}
          {% if seoPosts.length > 0 %}
            {% for post in seoPosts %}
            <div class="category-post-item">
              <a href="{{ post.url }}" class="category-post-link">{{ post.data.title }}</a>
            </div>
            {% endfor %}
          {% else %}
            <p class="category-empty">No posts in this category yet.</p>
          {% endif %}
        </div>
        <a href="/blog/?category=seo" class="view-all-link">View All Posts</a>
      </div>
      
      {# Digital Strategy Category #}
      <div class="category-panel">
        <div class="category-header">
          <div class="category-icon">DS</div>
          <div class="category-title-group">
            <h3>Digital Strategy</h3>
            {% set dsCount = collections.blog | filterByCategory('strategy') | length %}
            <span class="category-count">{{ dsCount }} Article{% if dsCount != 1 %}s{% endif %}</span>
          </div>
        </div>
        <p class="category-description">
          {{ homepage.categories.panels.strategy.description }}
        </p>
        <div class="category-posts">
          {% set strategyPosts = collections.blog | filterByCategory('strategy') | reverse | limit(3) %}
          {% if strategyPosts.length > 0 %}
            {% for post in strategyPosts %}
            <div class="category-post-item">
              <a href="{{ post.url }}" class="category-post-link">{{ post.data.title }}</a>
            </div>
            {% endfor %}
          {% else %}
            <p class="category-empty">No posts in this category yet.</p>
          {% endif %}
        </div>
        <a href="/blog/?category=strategy" class="view-all-link">View All Posts</a>
      </div>
      
      {# Tools & Automation Category #}
      <div class="category-panel">
        <div class="category-header">
          <div class="category-icon">TA</div>
          <div class="category-title-group">
            <h3>Tools & Automation</h3>
            {% set taCount = collections.blog | filterByCategory('tools') | length %}
            <span class="category-count">{{ taCount }} Article{% if taCount != 1 %}s{% endif %}</span>
          </div>
        </div>
        <p class="category-description">
          {{ homepage.categories.panels.tools.description }}
        </p>
        <div class="category-posts">
          {% set toolsPosts = collections.blog | filterByCategory('tools') | reverse | limit(3) %}
          {% if toolsPosts.length > 0 %}
            {% for post in toolsPosts %}
            <div class="category-post-item">
              <a href="{{ post.url }}" class="category-post-link">{{ post.data.title }}</a>
            </div>
            {% endfor %}
          {% else %}
            <p class="category-empty">No posts in this category yet.</p>
          {% endif %}
        </div>
        <a href="/blog/?category=tools" class="view-all-link">View All Posts</a>
      </div>
      
      {# Growth Hacking Category #}
      <div class="category-panel">
        <div class="category-header">
          <div class="category-icon">GH</div>
          <div class="category-title-group">
            <h3>Growth Hacking</h3>
            {% set ghCount = collections.blog | filterByCategory('growth') | length %}
            <span class="category-count">{{ ghCount }} Article{% if ghCount != 1 %}s{% endif %}</span>
          </div>
        </div>
        <p class="category-description">
          {{ homepage.categories.panels.growth.description }}
        </p>
        <div class="category-posts">
          {% set growthPosts = collections.blog | filterByCategory('growth') | reverse | limit(3) %}
          {% if growthPosts.length > 0 %}
            {% for post in growthPosts %}
            <div class="category-post-item">
              <a href="{{ post.url }}" class="category-post-link">{{ post.data.title }}</a>
            </div>
            {% endfor %}
          {% else %}
            <p class="category-empty">No posts in this category yet.</p>
          {% endif %}
        </div>
        <a href="/blog/?category=growth" class="view-all-link">View All Posts</a>
      </div>
    </div>
  </div>
</section>

{# ============================================
   CONTENT SECTIONS - Trending, Popular, New
   ============================================ #}
{# ============================================
   CONTENT SECTIONS - Trending, Popular, New
   ============================================ #}
<section class="content-sections">
  <div class="content-sections-container">
    <div class="section-header">
      <h2>{{ homepage.whatsHot.title }}</h2>
      <p>{{ homepage.whatsHot.subtitle }}</p>
    </div>
    
    <div class="section-tabs">
      <button class="tab-button active" onclick="openTab(event, 'trending')">Trending Now</button>
      <button class="tab-button" onclick="openTab(event, 'popular')">Most Shared</button>
      <button class="tab-button" onclick="openTab(event, 'new')">New This Week</button>
      <button class="tab-button" onclick="openTab(event, 'favorites')">Reader Favorites</button>
    </div>
    
    <!-- Tab Content: Trending -->
    <!-- Tab Content: Trending -->
    <div id="trending" class="tab-content" style="display: block;">
      <div class="featured-grid">
        {# Strict: Only posts marked as featured or tagged 'trending' #}
        {% set trendingPosts = collections.blog | filterByFeatured | reverse | limit(3) %}
        {% if trendingPosts.length > 0 %}
          {% for post in trendingPosts %}
            {% include "article-snippet.njk" %}
          {% endfor %}
        {% else %}
          <p class="section-empty-state">No trending posts right now. Check back soon!</p>
        {% endif %}
      </div>
    </div>

    <!-- Tab Content: Most Shared -->
    <div id="popular" class="tab-content" style="display: none;">
      <div class="featured-grid">
        {# Strict: Only posts tagged 'popular' #}
        {% set popularPosts = collections.blog | filterByTag('popular') | reverse | limit(3) %}
        {% if popularPosts.length > 0 %}
          {% for post in popularPosts %}
            {% include "article-snippet.njk" %}
          {% endfor %}
        {% else %}
          <p class="section-empty-state">No popular posts yet.</p>
        {% endif %}
      </div>
    </div>

    <!-- Tab Content: New This Week -->
    <div id="new" class="tab-content" style="display: none;">
      <div class="featured-grid">
        {# Strict: The 3 most recent posts #}
        {% set newPosts = collections.blog | reverse | limit(3) %}
        {% for post in newPosts %}
          {% include "article-snippet.njk" %}
        {% endfor %}
      </div>
    </div>

    <!-- Tab Content: Reader Favorites -->
    <div id="favorites" class="tab-content" style="display: none;">
      <div class="featured-grid">
        {# Strict: Only posts tagged 'favorite' #}
        {% set favoritePosts = collections.blog | filterByTag('favorite') | reverse | limit(3) %}
        {% if favoritePosts.length > 0 %}
          {% for post in favoritePosts %}
            {% include "article-snippet.njk" %}
          {% endfor %}
        {% else %}
          <p class="section-empty-state">No favorites marked yet.</p>
        {% endif %}
      </div>
    </div>
    
  </div>
</section>

<script>
function openTab(evt, tabName) {
  var i, tabcontent, tablinks;
  tabcontent = document.getElementsByClassName("tab-content");
  for (i = 0; i < tabcontent.length; i++) {
    tabcontent[i].style.display = "none";
  }
  tablinks = document.getElementsByClassName("tab-button");
  for (i = 0; i < tablinks.length; i++) {
    tablinks[i].className = tablinks[i].className.replace(" active", "");
  }
  document.getElementById(tabName).style.display = "block";
  evt.currentTarget.className += " active";
}
</script>

{# ============================================
   TOOLS & RESOURCES BAND - CAROUSEL
   ============================================ #}
<section class="tools-resources-band">
  <div class="tools-container">
    <div class="tools-header">
      <h2>{{ homepage.toolsSection.title }}</h2>
      <p>{{ homepage.toolsSection.subtitle }}</p>
    </div>
    
    <div class="tools-carousel-wrapper">
      <div class="tools-carousel">
        <div class="tools-carousel-track">
          <a href="https://tools.evolvedlotus.com/twitterreplybot/tweet-replier/" class="tool-card">
            <div class="tool-icon">🤖</div>
            <h3 class="tool-name">Twitter Reply Bot</h3>
            <p class="tool-description">Generate realistic Twitter replies with AI. 5 free uses daily.</p>
          </a>
          
          <a href="/blog/2025-10-22-plan-batch-and-grow-your-social-media/" class="tool-card">
            <div class="tool-icon">📅</div>
            <h3 class="tool-name">Content Calendar</h3>
            <p class="tool-description">Free templates for Notion, Google Sheets, and Instagram planning.</p>
          </a>
          
          <a href="https://tools.evolvedlotus.com" class="tool-card">
            <div class="tool-icon">🚀</div>
            <h3 class="tool-name">Growth Tools</h3>
            <p class="tool-description">Access our complete suite of marketing and automation tools.</p>
          </a>
          
          <a href="/blog/?category=templates" class="tool-card">
            <div class="tool-icon">📋</div>
            <h3 class="tool-name">Templates Library</h3>
            <p class="tool-description">Download ready-to-use templates and cheat sheets.</p>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const track = document.querySelector('.tools-carousel-track');
  const originalCards = document.querySelectorAll('.tool-card');
  
  if (!track || originalCards.length === 0) return;
  
  // Clone first and last cards for infinite loop
  const firstClone = originalCards[0].cloneNode(true);
  const lastClone = originalCards[originalCards.length - 1].cloneNode(true);
  
  // Add clones to DOM
  track.appendChild(firstClone);
  track.insertBefore(lastClone, originalCards[0]);
  
  const allCards = document.querySelectorAll('.tool-card');
  let currentIndex = 1; // Start at the first real card
  let isTransitioning = false;
  let cardWidth = originalCards[0].offsetWidth + 32; // card width + gap
  
  function updateCarousel(instant = false) {
    const offset = -currentIndex * cardWidth;
    track.style.transition = instant ? 'none' : 'transform 0.5s cubic-bezier(0.4, 0, 0.2, 1)';
    track.style.transform = `translateX(${offset}px)`;
    
    // Blur logic
    allCards.forEach((card, index) => {
      const dist = Math.abs(index - currentIndex);
      card.style.transition = 'all 0.5s ease';
      
      if (dist === 0) {
        card.style.opacity = '1';
        card.style.filter = 'blur(0px)';
        card.style.transform = 'scale(1)';
      } else if (dist === 1) {
        card.style.opacity = '0.6';
        card.style.filter = 'blur(1px)';
        card.style.transform = 'scale(0.95)';
      } else {
        card.style.opacity = '0.3';
        card.style.filter = 'blur(3px)';
        card.style.transform = 'scale(0.9)';
      }
    });
  }
  
  function nextSlide() {
    if (isTransitioning) return;
    isTransitioning = true;
    currentIndex++;
    updateCarousel();
  }
  
  // Handle transition end for infinite loop reset
  track.addEventListener('transitionend', () => {
    isTransitioning = false;
    
    // If we reached the clone of the first card (at the end)
    if (currentIndex === allCards.length - 1) {
      currentIndex = 1; // Jump back to the real first card
      updateCarousel(true);
    }
    
    // If we reached the clone of the last card (at the start)
    if (currentIndex === 0) {
      currentIndex = allCards.length - 2; // Jump to real last card
      updateCarousel(true);
    }
  });
  
  // Auto-cycle
  setInterval(nextSlide, 3000);
  
  // Handle resize
  window.addEventListener('resize', () => {
    cardWidth = originalCards[0].offsetWidth + 32;
    currentIndex = 1;
    updateCarousel(true);
  });
  
  // Initial setup
  setTimeout(() => {
    cardWidth = originalCards[0].offsetWidth + 32;
    updateCarousel(true);
  }, 100);
});
</script>

{# ============================================
   INSIGHTS PANEL (Redesigned)
   ============================================ #}
<section class="insights-panel">
  <div class="insights-container">
    <div class="section-header">
      <h2>{{ homepage.insights.title }}</h2>
      <p>{{ homepage.insights.subtitle }}</p>
    </div>
    
    <div class="insights-list">
      {% set insights = collections.insights | reverse | limit(3) %}
      {% if insights.length > 0 %}
        {% for insight in insights %}
        <div class="insight-item">
          <span class="insight-badge {{ insight.data.type | lower }}">{{ insight.data.type }}</span>
          <div class="insight-content">
            <h3>{{ insight.data.title }}</h3>
            <p>{{ insight.data.summary }}</p>
          </div>
          <a href="{{ insight.url }}" class="insight-link">Read More</a>
        </div>
        {% endfor %}
      {% else %}
        {# Fallback if no insights exist yet #}
        <div class="insight-item">
          <span class="insight-badge update">Update</span>
          <div class="insight-content">
            <h3>New Twitter Reply Bot Launched</h3>
            <p>We've just released our AI-powered Twitter reply tool. Try it for free today!</p>
          </div>
          <a href="https://tools.evolvedlotus.com/twitterreplybot/tweet-replier/" class="insight-link">Try Now</a>
        </div>
      {% endif %}
    </div>
  </div>
</section>

{# ============================================
   NEWSLETTER SIGNUP
   ============================================ #}
<section class="newsletter-section">
  <div class="newsletter-container">
    <h2>{{ homepage.newsletter.title }}</h2>
    <p>{{ homepage.newsletter.description }}</p>
    <form class="newsletter-form" action="https://mailchi.mp/78ca0b0ef493/newsletter" method="get" target="_blank">
      <input type="email" class="newsletter-input" placeholder="Enter your email" required>
      <button type="submit" class="newsletter-button">{{ homepage.newsletter.buttonText }}</button>
    </form>
  </div>
</section>

{# ============================================
   QUICK ACCESS BUTTONS
   ============================================ #}
<section class="quick-access">
  <div class="quick-access-container">
    <h2>{{ homepage.quickAccess.title }}</h2>
    <div class="quick-buttons">
      <a href="/blog/" class="quick-button">Browse Categories</a>
      {% set latestPost = collections.blog | reverse | first %}
      <a href="{{ latestPost.url }}" class="quick-button">Read the Latest</a>
      <button onclick="recommendRandom()" class="quick-button">Recommended For You</button>
    </div>
    <script>
      function recommendRandom() {
        const platform = prompt("Which platform are you interested in? (e.g., TikTok, YouTube, Instagram)");
        if (platform) {
          window.location.href = '/blog/?search=' + encodeURIComponent(platform);
        }
      }
    </script>
  </div>
</section>

{# ============================================
   TESTIMONIALS SECTION - Social Proof (Moved to bottom)
   ============================================ #}
<section class="testimonials-section">
  <div class="testimonials-container">
    <div class="section-header">
      <h2>{{ homepage.testimonials.title }}</h2>
      <p>{{ homepage.testimonials.subtitle }}</p>
    </div>
    
    <div class="testimonials-grid">
      {% for testimonial in homepage.testimonials.items %}
      <div class="testimonial-card">
        <p class="testimonial-text">
          "{{ testimonial.text }}"
        </p>
        <div class="testimonial-author">
          <div class="author-avatar">{{ testimonial.avatar }}</div>
          <div class="author-info">
            <div class="author-name">{{ testimonial.author }}</div>
            <div class="author-role">{{ testimonial.role }}</div>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>
