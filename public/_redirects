# Short URL API endpoint
/api/shorten    /.netlify/functions/shorten    200!

# Short URL redirects
/r/*    /.netlify/functions/shorten    200!

# Primary redirects - ensure no redirect chains
/blog/*    /blog/:splat    301
/    /index.html    200

# Handle 404s with a custom error page
/*    /404.html    404

# SEO-friendly redirects - prevent duplicate content
# No additional redirects needed as Eleventy handles URL structure
